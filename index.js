/**
 * index.js — App Entry Point
 *
 * Mendelegasikan sepenuhnya ke Expo Router.
 * Semua logic (AuthProvider, routing, guards) ada di app/_layout.js.
 *
 * JANGAN tambahkan registerRootComponent atau komponen lain di sini —
 * itu akan meng-override Expo Router dan menyebabkan "Unmatched Route".
 */
import 'expo-router/entry';
